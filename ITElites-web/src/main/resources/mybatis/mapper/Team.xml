<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.sicnu.itelites.dao.ITeamDAO">
    <cache eviction="FIFO" flushInterval="200"/>
    <insert id="insertTeam" useGeneratedKeys="true" keyColumn="team_id" keyProperty="teamId">
        INSERT INTO
        tb_team (team_id,team_name,create_time,last_edit_time)
        VALUES
        (#{teamId},#{teamName},#{createTime},#{lastEditTime});
    </insert>
    <update id="updateTeam" parameterType="cn.sicnu.itelites.entity.Team">
        UPDATE tb_team
        <set>
            <if test="teamName != null">team_name=#{teamName}</if>
            <if test="createTime != null">create_time=#{createTime}</if>
            <if test="last_edit_time != null">last_edit_time=#{lastEditTime}</if>
            <if test="team_desc != null">team_desc=#{teamDesc}</if>
        </set>
        WHERE team_id=#{teamId};
    </update>
    <select id="queryTeam" resultType="Team">
        SELECT
        team_id,team_name,create_time,last_edit_time,team_desc
        FROM
        tb_team;
    </select>
    <select id="queryTeamById" resultType="Team">
        SELECT
        team_id,team_name,create_time,last_edit_time,team_desc
        FROM
        tb_team;
    </select>
</mapper>